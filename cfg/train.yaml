seed: 0
num_envs: 2048
board_size: 10
device: cuda

epochs: 500
episode_len: 50

black_checkpoint: 
white_checkpoint:

wandb:
  group: board_${board_size}_${board_size}
  project: gomoku_rl
  mode: online # set to 'disabled' when debugging locally

defaults:
  - algo: ppo
