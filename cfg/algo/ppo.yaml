name: ppo

# ppo
ppo_epochs: 3
clip_param: 0.2 # 0.2
entropy_coef: 0.001 # 0.01
gae_lambda: 0.95
gamma: 0.99 # 0.99
max_grad_norm: 1.0

normalize_advantage: true

lr: 5e-4

actor:
  num_channels: 32
  num_residual_blocks: 3

critic:
  num_channels: 32
  num_residual_blocks: 3
